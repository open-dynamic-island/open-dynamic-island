{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal event access for the island webview",
  "windows": ["island"],
  "permissions": [
    "core:default",
    "core:event:allow-listen"
  ]
}
